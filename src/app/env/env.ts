export const environment = {
  production: true,
  baseUrl: '',
  affinda: {
    apiUrl: 'https://api.affinda.com/v3',
    apiKey: '', // Set this in production deployment
    workspaceIdentifier: '', // Set this in production deployment
    documentTypeIdentifier: '', // Optional: Set this in production deployment
  },
};
